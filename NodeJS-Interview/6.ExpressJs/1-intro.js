/*
     http is a very core way of client-server programming and 
     Express is a framework built on top of http module makes
     life easier to do the same thing.
     Express has:
        - less chance of failure
        - work asynchronusly
        - better performance
    npm install --save express
     npm -> node package manager
     install -> command to install a package
     --save -> flag to save the package in package.json 
*/
